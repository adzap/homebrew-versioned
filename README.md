# Adzap Versioned

## How do I install these formulae?
`brew install adzap/versioned/<formula>`

Or `brew tap adzap/versioned` and then `brew install <formula>`.

## Documentation
`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
